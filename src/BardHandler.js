/**
 * Generate code using Google Bard and insert it in place of the Bard comment.
 * @param {string} prompt The content of the Google Bard prompt.
 */
const Run = async (prompt) => 
{
    console.log(prompt)
}

module.exports = { Run };